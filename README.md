
To start the project, activate the virtual environment on your terminal:

```bash
source .venv/bin/activate
```

Then install the required libraries (only needed the first time, or when
`requirements.txt` changes):

```bash
pip install -r requirements.txt
```

Check it worked:

```bash
pip list
```
